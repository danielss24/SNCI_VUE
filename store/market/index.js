export const state = () => ({
    market : []
})

export const getters = {
    get: (state) => {
        return state.market
    }
}

export const mutations = {
    add (state, item)  {
        const itemAux = {
            id: itemAux.id,
            name: itemAux.name,
            quantity: itemAux.quantity,
            valoration: itemAux.valoration,
            contVal: contVal+1
        }
    }
}
export const actions = {
    getMarketFromFB () {
        
    }
}